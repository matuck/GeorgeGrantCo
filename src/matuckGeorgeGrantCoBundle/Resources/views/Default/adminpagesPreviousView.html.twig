{% extends '::base.html.twig' %}

{% block body %}
    <h1 class="alert">This is a previous version</h1>
    <h1>{{ page.title }}</h1>
    <div>
        {{ page.content|raw }}
    </div>
    <div>
        <a href="{{ path('adminpagesPrevious', {'id': page.id }) }}" class="pull-right btn btn-primary" role="button">Previous Version</a>
        <a href="{{ path('adminpages') }}" class="pull-right btn btn-primary" role="button">Page List</a>
        <a href="{{ path('adminpagesPreviousRevert', {'id': page.id, 'version': version }) }}" class="pull-right btn btn-primary" role="button">Revert</a>
    </div>
{% endblock %}
